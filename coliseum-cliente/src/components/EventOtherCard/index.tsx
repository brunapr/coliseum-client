import React, { useState } from 'react';
import { View, Text, Button } from 'react-native';

import { Card, DivImage} from './styles';

export default function EventOtherCard(props:any) {

    return (
        < Card>
            <DivImage>

            </DivImage>
                        
        </Card>

    );
}